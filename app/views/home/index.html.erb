<div class="banner">
    <h1>Explore your world.</h1>
</div>

<div class="row">
    <% @cities.each do |city| %>
    <div class="col s12 m4 center">
        <div class="card">
            <div class="card-image">
                <%= link_to image_tag('http://lorempixel.com/800/400/food/1'), city %>
                <span class="card-title"><%= link_to city.name, city %></span>
            </div>
        </div>
    </div>
    <% end %>
</div>
