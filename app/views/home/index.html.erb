<!-- <% @cities.each do |city| %>
  <p><%= city.name %></p>
  <p><%= city.country %></p>
  <p><<%= city.photoURL %></p>
  <%= link_to 'show', city_path(city) %>
  <%= image_tag city.photoURL, class: "city-photo" %>
<% end %> -->

<% @cities.each do |c| %>

    <div class="row">
      <div class="col s12 m7">
        <div class="card">
          <div class="card-image">
            <%= image_tag c.photoURL %> %>
            <span class="card-title"><%= c.name %></span>
          </div>
        </div>
      </div>
    </div>

<% end %>
