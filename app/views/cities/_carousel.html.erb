<!-- Carousel partial -->
<div class="carousel-div">
  <div class="slider">

    <ul class="slides">
      <%= @cities.each do |city| %>
         <li>
           <%= link_to (image_tag(city.photoURL)), city_path(city) %>
         </li>
      <% end %>
    </ul>

  </div>
</div>
